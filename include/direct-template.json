{
  "apiVersion": "v1",
  "kind": "Template",
  "metadata": {
    "labels": {
      "app": "enmasse"
    },
    "name": "direct"
  },
  "objects": [
    {
      "apiVersion": "v1",
      "kind": "ConfigMap",
      "metadata": {
        "name": "${NAME}",
        "labels": {
          "app": "enmasse",
          "type": "address-config"
        }
      }
    }
  ],
  "parameters": [
    {
      "name": "NAME",
      "description": "A valid name for the instance",
      "required": true
    },
    {
      "name": "ADDRESS",
      "description": "The address to use for the address",
      "required": true
    },
    {
      "name": "ADDRESS_LIST",
      "description": "The list of addresses to use for this flavor",
      "required": false
    }
  ]
}
